apply plugin: 'java'
apply plugin: 'maven'

dependencies {
	testCompile 'junit:junit:4.12'
}

repositories {
	mavenCentral()
}

task version {
	group 'Help'
	description 'Displays the project version'
	doLast {
		println version
	}
}

task javadocJar(type: Jar) {
	classifier = 'javadoc'
	from javadoc
}

artifacts {
	archives javadocJar
}
